services:

    openrefine-alignements:
        image: abes/openrefine376
        profiles: ["refine-alignements"]
        volumes:
            - ./volumes/refine-alignements:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13334:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"

    openrefine-bacon:
        image: abes/openrefine376
        profiles: ["refbacon"]
        volumes:
            - ./volumes/refbacon:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13336:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"

    openrefine-hub:
        image: abes/openrefine376
        profiles: ["refhub"]
        volumes:
            - ./volumes/refhub:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13337:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
      
    openrefine-idref:
        image: abes/openrefine376
        profiles: ["refidref"]
        volumes:
            - ./volumes/refidref:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13338:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"

      
    openrefine-movies:
        image: abes/openrefine376
        profiles: ["refmovies"]
        volumes:
            - ./volumes/refmovies:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13341:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
      
    openrefine-reforcid:
        image: abes/openrefine-3.7.7
        profiles: ["reforcid"]
        volumes:
            - ./volumes/reforcid:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13340:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
      
    openrefine-refperios:
        image: abes/openrefine376
        profiles: ["refine-perios"]
        volumes:
            - ./volumes/refine-perios:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13333:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
      
    openrefine-sudoc:
        image: abes/openrefine376
        profiles: ["refsudoc"]
        volumes:
            - ./volumes/refsudoc:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13339:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
      
    openrefine-theses:
        image: abes/openrefine376
        profiles: ["reftheses"]
        volumes:
            - ./volumes/reftheses:/mnt/refine
            - ./refine.ini:/./openrefine-$RELEASE/refine.ini
        ports:
            - "13340:3333"
        env_file:
            - openrefine.env
        restart: unless-stopped
        labels:
      # pour que les mises à jour de cette image soient auto-déployées par watchtower
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
      

    movies-watchtower:
    image: containrrr/watchtower:1.4.0
    container_name: openrefine-watchtower
    restart: unless-stopped
    networks:
      - default
    mem_limit: ${OPENREFINE_MEM_LIMIT}
    memswap_limit: ${OPENREFINE_MEMSWAP_LIMIT}
    cpus: ${OPENREFINE_CPU_LIMIT}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      WATCHTOWER_CLEANUP: "true"
      WATCHTOWER_POLL_INTERVAL: 60
      WATCHTOWER_DEBUG: "false"
      WATCHTOWER_NO_STARTUP_MESSAGE: "true"
      WATCHTOWER_WARN_ON_HEAD_FAILURE: "never"
      WATCHTOWER_RUN_ONCE: ${OPENREFINE_WATCHTOWER_RUN_ONCE}
      WATCHTOWER_SCOPE: "openrefine-watchtower-scope"
    labels:
      - "com.centurylinklabs.watchtower.scope=openrefine-watchtower-scope"
